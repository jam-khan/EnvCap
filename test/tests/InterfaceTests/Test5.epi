@resource interface Utils

val inc : Bool -> String -> Int;
val f   : Sig[Int, Int -> Int];
val x   : {int: Int, bool: Bool, str: String};

functor temp(y: Int) {
    function hello(x: Int) : Int -> Int;
    functor inside(y: Int) {
        val useless : Bool
    }
};

module temp {
    val x : Int
}