@resource module main

import InsertionSort MergeSort QuickSort Listutils;

open MergeSort(Listutils);
open QuickSort(Listutils);
open InsertionSort(Listutils);

val example = [4, 3, 2, 1]<Int>;

val sortedM = mergeSort(example);
val sortedI = insertionSort(example);
val sortedQ = quickSort(example)
