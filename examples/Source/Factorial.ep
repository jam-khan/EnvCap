module factorial where
    fib : Int -> Int = 
        (\n: Int) => {
            if (n <= 1) {
                return n;
            } else {
                return fib (n - 1) + fib (n - 2);
            };
        }

    main : Void = print . (show (fib 13))
