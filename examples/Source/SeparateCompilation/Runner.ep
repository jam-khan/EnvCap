@secure module Runner {
    require utils : import "Utils",
    require sort  : import "Sort"
} 
{
    open sort(utils, 1, 2, {"z" = 2});
    val finalResult : Int = result;    
    -- val result : Int = 10  // Not allowed due to name clash after open
};

-- This is an executable essentially for IO
main : IO() {
    print Runner.finalResult
}