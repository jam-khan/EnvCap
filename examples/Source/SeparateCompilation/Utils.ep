@secure module Utils {
    require x : Int
}
{
    function add(y : Int) : Int { x + y };
    function factorial(n : Int) : Int {
        if (n == 0) then { 1 }
                    else { n * factorial(n - 1) }
    }
}