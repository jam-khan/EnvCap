
letrec 
    {
    factorial : Int -> Int = 
            \(n:Int) => { if (n == 0) 
                                then { 1 }
                                else { n * factorial(n - 1) } };
    result : Int = factorial(10);
    }
    in { result }