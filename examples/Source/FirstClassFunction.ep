function identity(n: Int) {
    n + 10
};

function fibonacci(call: Int -> Int) {
    call(10)
};

fibonacci(identity)


function identity(i : Int) { i + 1 };

function fibonacci(n: Int, id: Int -> Int) {
    if (n == 0) then {
        id(n)
    } else {
        fibonacci(n - 1, id) + fibonacci(n - 2, id)
    }
};

fibonacci(0, identity)